[project]
name = "kubecon-na-2025-demo"
version = "0.1.0"
description = "KubeCon North America 2025 Demo using Agent Framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "agent-framework>=1.0.0b251001",
]

[tool.uv]
prerelease = "allow"
